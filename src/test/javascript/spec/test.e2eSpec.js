describe('angularjs homepage', function() {
    it('should greet the named user', function() {
       // var welcomText = element(by.binding('yourName'));
        //expect(greeting.getText()).toEqual('Hello Julie!');
    });
});